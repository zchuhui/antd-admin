import modelExtend from 'dva-model-extend'
import { model } from 'models/common'
/* import { } from 'services/rival' */

import { message } from 'antd'

export default modelExtend(model, {
    namespace: 'sort',

    state: {
        loading:true,
    },

    reducers: {
        
    },

    effects: {
       
    }
});

